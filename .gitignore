# Captured logs — contain cookies, tokens and page content. Never commit.
data/
*.jsonl
*.jsonl.zst

# Dependencies
node_modules/

# Environment-specific notes (IPs, hostnames, accounts) — never publish
*.local.md
